We can use open ai deepresearch api (https://platform.openai.com/docs/models/o3-deep-research) to get a list of links and initial analysis.
Then we can dive deeper by ingesting each linked pdf into our system
and running our own vision agent on it. 

We can make all the citations buttons that show excerpt in the source document.

